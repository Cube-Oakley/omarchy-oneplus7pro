--- a/arch/arm64/kernel/bringup_usb.c
+++ b/arch/arm64/kernel/bringup_usb.c
@@ -312,32 +312,6 @@
 	of_node_put(np);
 }
 
-static void bringup_halt_leftover_mdp(void)
-{
-	void __iomem *dpu;
-	u32 te;
-
-	/* SM8150 DPU @ 0xae01000; INTF_1 (DSI0) timing engine @ +0x6a800. */
-	dpu = ioremap(0x0ae01000UL, 0x80000);
-	if (!dpu) {
-		pr_err("halt leftover: ioremap dpu failed\n");
-		return;
-	}
-	te = readl(dpu + 0x6a800);
-	pr_info("halt leftover: INTF_1 TE was 0x%x\n", te);
-	writel(0, dpu + 0x6a000); /* INTF_0 */
-	writel(0, dpu + 0x6a800); /* INTF_1 TIMING_ENGINE_EN */
-	writel(0, dpu + 0x6b000); /* INTF_2 */
-	writel(0, dpu + 0x6b800); /* INTF_3 */
-	writel(0, dpu + 0x6a800 + 0x284); /* INTF_1 TEAR_CHECK */
-	writel(0, dpu + 0x6b000 + 0x284); /* INTF_2 TEAR_CHECK */
-	wmb();
-	iounmap(dpu);
-	if (bringup_fb)
-		bringup_fb_fill(0xFF00FF00, 0, FB_H);
-	pr_info("halt leftover: INTF TE off, leftover FB green\n");
-}
-
 static void bringup_gpu_work(struct work_struct *work)
 {
 	pr_info("gpu delayed: enabling gmu+adreno\n");
@@ -349,12 +323,9 @@
 {
 	struct device_node *np;
 
-	/*
-	 * Leftover ABL scanout dies when dispcc reprograms MDSS clocks.
-	 * USB is already up; Hyprland has had time to paint on simpledrm.
-	 * No panel node yet: we want DPU/DSI probe logs and a CRTC if the
-	 * host comes up without a panel driver.
-	 */
+	/* USB is already available. Stop the boot framebuffer console before
+	 * the native driver takes ownership of panel clocks and scanout. */
+	unregister_console(&bringup_fb_cons);
 	pr_info("dpu delayed: enabling dispcc+mdss+dsi0\n");
 	bringup_enable_compat("qcom,sm8150-dispcc");
 	msleep(250);
@@ -378,7 +349,6 @@
 	bringup_dump_compat("qcom,sm8150-dpu");
 	bringup_dump_compat("qcom,sm8150-dsi-ctrl");
 	bringup_dump_compat("qcom,dsi-phy-7nm-8150");
-	bringup_halt_leftover_mdp();
 }
 
 static DECLARE_DELAYED_WORK(bringup_gpu_dwork, bringup_gpu_work);
@@ -438,12 +408,13 @@
 	 */
 	bringup_dump_compat("qcom,sm8150-dispcc");
 	bringup_dump_compat("qcom,sm8150-mdss");
-	schedule_delayed_work(&bringup_simpledrm_dwork, 25 * HZ);
+	if (!bringup_dpu)
+		schedule_delayed_work(&bringup_simpledrm_dwork, 25 * HZ);
 	schedule_delayed_work(&bringup_gpu_dwork, 90 * HZ);
 	if (bringup_dpu)
 		schedule_delayed_work(&bringup_dpu_dwork, 120 * HZ);
-	pr_info("simpledrm delayed 25s, gpu delayed 90s, dpu %s\n",
-		bringup_dpu ? "delayed 120s" : "disabled");
+	pr_info("scanout=%s, gpu delayed 90s\n",
+		bringup_dpu ? "native DPU delayed 120s" : "simpledrm delayed 25s");
 	return 0;
 }
 late_initcall_sync(bringup_usb_init);
--- a/drivers/gpu/drm/msm/msm_kms.c
+++ b/drivers/gpu/drm/msm/msm_kms.c
@@ -6,7 +6,6 @@
  */
 
 #include <linux/aperture.h>
-#include <linux/of_device.h>
 #include <linux/kthread.h>
 #include <linux/sched/mm.h>
 #include <uapi/linux/sched/types.h>
@@ -192,28 +191,14 @@
 	 * IOMMUs can be a part of MDSS device tree binding, or the
 	 * MDP/DPU device.
 	 */
-	{
-		struct device *try[2] = { mdp_dev, mdss_dev };
-		int i;
-
-		mmu = NULL;
-		for (i = 0; i < 2; i++) {
-			iommu_dev = try[i];
-			if (!iommu_dev)
-				continue;
-			if (!device_iommu_mapped(iommu_dev) && iommu_dev->of_node)
-				of_dma_configure(iommu_dev, iommu_dev->of_node, true);
-			drm_info(dev, "kms iommu try %s mapped=%d\n",
-				 dev_name(iommu_dev), device_iommu_mapped(iommu_dev));
-			mmu = msm_iommu_disp_new(iommu_dev, 0);
-			if (!IS_ERR_OR_NULL(mmu))
-				break;
-			if (IS_ERR(mmu))
-				drm_err(dev, "iommu_disp_new %s: %pe\n",
-					dev_name(iommu_dev), mmu);
-			mmu = NULL;
-		}
-	}
+	if (device_iommu_mapped(mdp_dev))
+		iommu_dev = mdp_dev;
+	else
+		iommu_dev = mdss_dev;
+
+	mmu = msm_iommu_disp_new(iommu_dev, 0);
+	if (IS_ERR(mmu))
+		return ERR_CAST(mmu);
 
 	if (!mmu) {
 		drm_info(dev, "no IOMMU, fallback to phys contig buffers for scanout\n");
--- a/drivers/gpu/drm/msm/msm_fbdev.c
+++ b/drivers/gpu/drm/msm/msm_fbdev.c
@@ -93,12 +93,6 @@
 {
 	struct drm_device *dev = helper->dev;
 	struct msm_drm_private *priv = dev->dev_private;
-
-	/* KMS-only drm_gpuvm oopses in get_vma_locked when pinning the
-	 * fbdev GEM BO (msm.separate_gpu_kms). Skip fbdev; Hyprland uses KMS.
-	 */
-	DRM_DEV_INFO(dev->dev, "skipping msm fbdev (GEM IOVA unsafe on KMS-only VM)\n");
-	return -ENOTSUPP;
 	struct drm_framebuffer *fb = NULL;
 	struct drm_gem_object *bo;
 	struct fb_info *fbi = NULL;
--- a/drivers/gpu/drm/msm/dsi/dsi_host.c
+++ b/drivers/gpu/drm/msm/dsi/dsi_host.c
@@ -166,6 +166,7 @@
 
 	struct drm_display_mode *mode;
 	struct drm_dsc_config *dsc;
+	unsigned int dsc_slice_per_pkt;
 
 	/* connected device info */
 	unsigned int channel;
@@ -910,17 +911,10 @@
 	slice_per_intf = dsc->slice_count;
 
 	total_bytes_per_intf = dsc->slice_chunk_size * slice_per_intf;
-	bytes_per_pkt = dsc->slice_chunk_size; /* * slice_per_pkt; */
+	bytes_per_pkt = dsc->slice_chunk_size * msm_host->dsc_slice_per_pkt;
 
 	eol_byte_num = total_bytes_per_intf % 3;
-
-	/*
-	 * Typically, pkt_per_line = slice_per_intf * slice_per_pkt.
-	 *
-	 * Since the current driver only supports slice_per_pkt = 1,
-	 * pkt_per_line will be equal to slice per intf for now.
-	 */
-	pkt_per_line = slice_per_intf;
+	pkt_per_line = slice_per_intf / msm_host->dsc_slice_per_pkt;
 
 	if (is_cmd_mode) /* packet data type */
 		reg = DSI_COMMAND_COMPRESSION_MODE_CTRL_STREAM0_DATATYPE(MIPI_DSI_DCS_LONG_WRITE);
@@ -1069,12 +1063,9 @@
 		else
 			/*
 			 * When DSC is enabled, WC = slice_chunk_size * slice_per_pkt + 1.
-			 * Currently, the driver only supports default value of slice_per_pkt = 1
-			 *
-			 * TODO: Expand mipi_dsi_device struct to hold slice_per_pkt info
-			 *       and adjust DSC math to account for slice_per_pkt.
 			 */
-			wc = msm_host->dsc->slice_chunk_size + 1;
+			wc = msm_host->dsc->slice_chunk_size *
+				msm_host->dsc_slice_per_pkt + 1;
 
 		dsi_write(msm_host, REG_DSI_CMD_MDP_STREAM0_CTRL,
 			DSI_CMD_MDP_STREAM0_CTRL_WORD_COUNT(wc) |
@@ -1202,13 +1193,31 @@
 
 int dsi_tx_buf_alloc_6g(struct msm_dsi_host *msm_host, int size)
 {
-	/*
-	 * KMS-only drm_gpuvm (msm.separate_gpu_kms) oopses in
-	 * msm_gem_get_and_pin_iova_range/get_vma_locked on guacamole.
-	 * Use dma_alloc_coherent like DSI v2.
-	 */
-	pr_info("dsi: TX buf via dma_alloc_coherent (skip GEM IOVA)\n");
-	return dsi_tx_buf_alloc_v2(msm_host, size);
+	struct drm_device *dev = msm_host->dev;
+	struct msm_drm_private *priv = dev->dev_private;
+	uint64_t iova;
+	u8 *data;
+
+	/* Native test requires a valid display IOMMU, never physical DMA. */
+	if (!priv->kms->vm)
+		return -ENODEV;
+
+	msm_host->vm = drm_gpuvm_get(priv->kms->vm);
+
+	data = msm_gem_kernel_new(dev, size, MSM_BO_WC,
+					msm_host->vm,
+					&msm_host->tx_gem_obj, &iova);
+
+	if (IS_ERR(data)) {
+		msm_host->tx_gem_obj = NULL;
+		return PTR_ERR(data);
+	}
+
+	msm_gem_object_set_name(msm_host->tx_gem_obj, "tx_gem");
+
+	msm_host->tx_size = msm_host->tx_gem_obj->size;
+
+	return 0;
 }
 
 int dsi_tx_buf_alloc_v2(struct msm_dsi_host *msm_host, int size)
@@ -1253,8 +1262,6 @@
 
 void *dsi_tx_buf_get_6g(struct msm_dsi_host *msm_host)
 {
-	if (msm_host->tx_buf)
-		return msm_host->tx_buf;
 	return msm_gem_get_vaddr(msm_host->tx_gem_obj);
 }
 
@@ -1265,8 +1272,6 @@
 
 void dsi_tx_buf_put_6g(struct msm_dsi_host *msm_host)
 {
-	if (msm_host->tx_buf)
-		return;
 	msm_gem_put_vaddr(msm_host->tx_gem_obj);
 }
 
@@ -1376,11 +1381,6 @@
 
 	if (!dma_base)
 		return -EINVAL;
-
-	if (msm_host->tx_buf) {
-		*dma_base = msm_host->tx_buf_paddr;
-		return 0;
-	}
 
 	return msm_gem_get_and_pin_iova(msm_host->tx_gem_obj,
 				priv->kms->vm, dma_base);
@@ -1678,8 +1678,11 @@
 	msm_host->lanes = dsi->lanes;
 	msm_host->format = dsi->format;
 	msm_host->mode_flags = dsi->mode_flags;
-	if (dsi->dsc)
+	if (dsi->dsc) {
 		msm_host->dsc = dsi->dsc;
+		/* For backwards compatibility, assume one slice if not set. */
+		msm_host->dsc_slice_per_pkt = dsi->dsc->dsc_slice_per_pkt ?: 1;
+	}
 
 	ret = dsi_dev_attach(msm_host->pdev);
 	if (ret)
--- a/drivers/gpu/drm/panel/Kconfig
+++ b/drivers/gpu/drm/panel/Kconfig
@@ -855,6 +855,13 @@
 	select DRM_MIPI_DSI
 	select VIDEOMODE_HELPERS
 
+config DRM_PANEL_SAMSUNG_ONEPLUS_DSC
+	tristate "Samsung OnePlus DSC command-mode panel"
+	depends on OF && DRM_MIPI_DSI && BACKLIGHT_CLASS_DEVICE
+	select DRM_DISPLAY_DSC_HELPER
+	help
+	  Experimental 1440x3120 DSC panel support for OnePlus phones.
+
 config DRM_PANEL_SAMSUNG_SOFEF00
 	tristate "Samsung sofef00/s6e3fc2x01 OnePlus 6/6T DSI cmd mode panels"
 	depends on OF
--- a/drivers/gpu/drm/panel/Makefile
+++ b/drivers/gpu/drm/panel/Makefile
@@ -115,3 +115,5 @@
 obj-$(CONFIG_DRM_PANEL_VISIONOX_R66451) += panel-visionox-r66451.o
 obj-$(CONFIG_DRM_PANEL_WIDECHIPS_WS2401) += panel-widechips-ws2401.o
 obj-$(CONFIG_DRM_PANEL_XINPENG_XPP055C272) += panel-xinpeng-xpp055c272.o
+
+obj-$(CONFIG_DRM_PANEL_SAMSUNG_ONEPLUS_DSC) += panel-samsung-oneplus-dsc.o
--- /dev/null
+++ b/drivers/gpu/drm/panel/panel-samsung-oneplus-dsc.c
@@ -0,0 +1,353 @@
+// SPDX-License-Identifier: GPL-2.0-only
+// Copyright (c) 2026 hotdog-linux-bringup contributors
+// Generated with linux-mdss-dsi-panel-driver-generator from vendor device tree:
+//   Copyright (c) 2015-2017, The Linux Foundation. All rights reserved.
+
+#include <linux/backlight.h>
+#include <linux/delay.h>
+#include <linux/gpio/consumer.h>
+#include <linux/mod_devicetable.h>
+#include <linux/module.h>
+#include <linux/regulator/consumer.h>
+
+#include <video/mipi_display.h>
+
+#include <drm/display/drm_dsc.h>
+#include <drm/display/drm_dsc_helper.h>
+#include <drm/drm_mipi_dsi.h>
+#include <drm/drm_modes.h>
+#include <drm/drm_panel.h>
+#include <drm/drm_probe_helper.h>
+
+struct samsung_oneplus_dsc {
+	struct drm_panel panel;
+	struct mipi_dsi_device *dsi;
+	struct drm_dsc_config dsc;
+	struct regulator_bulk_data *supplies;
+	struct gpio_desc *reset_gpio;
+};
+
+static const struct regulator_bulk_data samsung_oneplus_dsc_supplies[] = {
+	{ .supply = "vddio" },
+	{ .supply = "avdd" },
+};
+
+static inline
+struct samsung_oneplus_dsc *to_samsung_oneplus_dsc(struct drm_panel *panel)
+{
+	return container_of_const(panel, struct samsung_oneplus_dsc, panel);
+}
+
+static void samsung_oneplus_dsc_reset(struct samsung_oneplus_dsc *ctx)
+{
+	gpiod_set_value_cansleep(ctx->reset_gpio, 0);
+	usleep_range(8000, 9000);
+	gpiod_set_value_cansleep(ctx->reset_gpio, 1);
+	usleep_range(1000, 2000);
+	gpiod_set_value_cansleep(ctx->reset_gpio, 0);
+	usleep_range(5000, 6000);
+}
+
+static int samsung_oneplus_dsc_on(struct samsung_oneplus_dsc *ctx)
+{
+	struct mipi_dsi_multi_context dsi_ctx = { .dsi = ctx->dsi };
+
+	ctx->dsi->mode_flags |= MIPI_DSI_MODE_LPM;
+
+	mipi_dsi_compression_mode_multi(&dsi_ctx, true);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xc3, 0x00);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0x0a,
+				     0x10, 0x00, 0x00, 0x89, 0x30, 0x80, 0x0c,
+				     0x30, 0x05, 0xa0, 0x00, 0x41, 0x02, 0xd0,
+				     0x02, 0xd0, 0x02, 0x00, 0x02, 0xc2, 0x00,
+				     0x20, 0x06, 0x58, 0x00, 0x0a, 0x00, 0x0f,
+				     0x01, 0xe0, 0x01, 0x2d, 0x18, 0x00, 0x10,
+				     0xf0, 0x03, 0x0c, 0x20, 0x00, 0x06, 0x0b,
+				     0x0b, 0x33, 0x0e, 0x1c, 0x2a, 0x38, 0x46,
+				     0x54, 0x62, 0x69, 0x70, 0x77, 0x79, 0x7b,
+				     0x7d, 0x7e, 0x01, 0x02, 0x01, 0x00, 0x09,
+				     0x40, 0x09, 0xbe, 0x19, 0xfc, 0x19, 0xfa,
+				     0x19, 0xf8, 0x1a, 0x38, 0x1a, 0x78, 0x1a,
+				     0xb6, 0x2a, 0xb6, 0x2a, 0xf4, 0x2a, 0xf4,
+				     0x4b, 0x34, 0x63, 0x74, 0x00);
+	mipi_dsi_dcs_exit_sleep_mode_multi(&dsi_ctx);
+	mipi_dsi_usleep_range(&dsi_ctx, 10000, 11000);
+	mipi_dsi_dcs_set_tear_on_multi(&dsi_ctx, MIPI_DSI_DCS_TEAR_MODE_VBLANK);
+	mipi_dsi_dcs_set_column_address_multi(&dsi_ctx, 0x0000, 0x059f);
+	mipi_dsi_dcs_set_page_address_multi(&dsi_ctx, 0x0000, 0x0c2f);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0x5a, 0x5a);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb9,
+				     0x01, 0xc0, 0x3c, 0x0b, 0x00, 0x00, 0x00,
+				     0x11, 0x03);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0xa5, 0xa5);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0x5a, 0x5a);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb0, 0x45);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb5, 0x48);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0xa5, 0xa5);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0x5a, 0x5a);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfc, 0x5a, 0x5a);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb0, 0x1e);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xc5,
+				     0x09, 0x10, 0xb4, 0x24, 0xfb);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0xa5, 0xa5);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfc, 0xa5, 0xa5);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0x5a, 0x5a);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfc, 0x5a, 0x5a);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb0, 0x37);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xc5,
+				     0x04, 0xff, 0x00, 0x01, 0x64);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0xa5, 0xa5);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfc, 0xa5, 0xa5);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfc, 0x5a, 0x5a);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb0, 0x86);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xeb, 0x01);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xfc, 0xa5, 0xa5);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0x5a, 0x5a);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb0, 0x05);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb1, 0x01);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb0, 0x02);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xb5, 0xd3);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, MIPI_DCS_WRITE_CONTROL_DISPLAY,
+				     0x20);
+	mipi_dsi_dcs_write_seq_multi(&dsi_ctx, 0xf0, 0xa5, 0xa5);
+	mipi_dsi_dcs_set_display_on_multi(&dsi_ctx);
+
+	return dsi_ctx.accum_err;
+}
+
+static int samsung_oneplus_dsc_off(struct samsung_oneplus_dsc *ctx)
+{
+	struct mipi_dsi_multi_context dsi_ctx = { .dsi = ctx->dsi };
+
+	ctx->dsi->mode_flags &= ~MIPI_DSI_MODE_LPM;
+
+	mipi_dsi_dcs_set_display_off_multi(&dsi_ctx);
+	mipi_dsi_usleep_range(&dsi_ctx, 10000, 11000);
+	mipi_dsi_dcs_enter_sleep_mode_multi(&dsi_ctx);
+	mipi_dsi_msleep(&dsi_ctx, 120);
+
+	return dsi_ctx.accum_err;
+}
+
+static int samsung_oneplus_dsc_prepare(struct drm_panel *panel)
+{
+	struct samsung_oneplus_dsc *ctx = to_samsung_oneplus_dsc(panel);
+	struct drm_dsc_picture_parameter_set pps;
+	struct device *dev = &ctx->dsi->dev;
+	int ret;
+
+	ret = regulator_bulk_enable(ARRAY_SIZE(samsung_oneplus_dsc_supplies),
+				    ctx->supplies);
+	if (ret < 0) {
+		dev_err(dev, "Failed to enable regulators: %d\n", ret);
+		return ret;
+	}
+	msleep(20);
+
+	samsung_oneplus_dsc_reset(ctx);
+
+	ret = samsung_oneplus_dsc_on(ctx);
+	if (ret < 0) {
+		dev_err(dev, "Failed to initialize panel: %d\n", ret);
+		gpiod_set_value_cansleep(ctx->reset_gpio, 1);
+		regulator_bulk_disable(ARRAY_SIZE(samsung_oneplus_dsc_supplies),
+				       ctx->supplies);
+		return ret;
+	}
+
+	/* The downstream display path sends a generated PPS after panel-on. */
+	dev_info(dev, "DSC %ux%u slice=%ux%u count=%u bpp=%u/16 chunk=%u\n",
+		 ctx->dsc.pic_width, ctx->dsc.pic_height, ctx->dsc.slice_width,
+		 ctx->dsc.slice_height, ctx->dsc.slice_count,
+		 ctx->dsc.bits_per_pixel, ctx->dsc.slice_chunk_size);
+	drm_dsc_pps_payload_pack(&pps, &ctx->dsc);
+	ret = mipi_dsi_picture_parameter_set(ctx->dsi, &pps);
+	if (ret < 0) {
+		dev_err(dev, "Failed to transmit DSC PPS: %d\n", ret);
+		gpiod_set_value_cansleep(ctx->reset_gpio, 1);
+		regulator_bulk_disable(ARRAY_SIZE(samsung_oneplus_dsc_supplies),
+					       ctx->supplies);
+		return ret;
+	}
+
+	return 0;
+}
+
+static int samsung_oneplus_dsc_unprepare(struct drm_panel *panel)
+{
+	struct samsung_oneplus_dsc *ctx = to_samsung_oneplus_dsc(panel);
+	struct device *dev = &ctx->dsi->dev;
+	int ret;
+
+	ret = samsung_oneplus_dsc_off(ctx);
+	if (ret < 0)
+		dev_err(dev, "Failed to un-initialize panel: %d\n", ret);
+
+	gpiod_set_value_cansleep(ctx->reset_gpio, 1);
+	regulator_bulk_disable(ARRAY_SIZE(samsung_oneplus_dsc_supplies),
+			       ctx->supplies);
+
+	return 0;
+}
+
+static const struct drm_display_mode samsung_oneplus_dsc_mode = {
+	.clock = (1440 + 16 + 8 + 8) * (3120 + 400 + 28 + 1156) * 60 / 1000,
+	.hdisplay = 1440,
+	.hsync_start = 1440 + 16,
+	.hsync_end = 1440 + 16 + 8,
+	.htotal = 1440 + 16 + 8 + 8,
+	.vdisplay = 3120,
+	.vsync_start = 3120 + 400,
+	.vsync_end = 3120 + 400 + 28,
+	.vtotal = 3120 + 400 + 28 + 1156,
+	.width_mm = 71,
+	.height_mm = 154,
+	.type = DRM_MODE_TYPE_DRIVER,
+};
+
+static int samsung_oneplus_dsc_get_modes(struct drm_panel *panel,
+					 struct drm_connector *connector)
+{
+	return drm_connector_helper_get_modes_fixed(connector, &samsung_oneplus_dsc_mode);
+}
+
+static const struct drm_panel_funcs samsung_oneplus_dsc_panel_funcs = {
+	.prepare = samsung_oneplus_dsc_prepare,
+	.unprepare = samsung_oneplus_dsc_unprepare,
+	.get_modes = samsung_oneplus_dsc_get_modes,
+};
+
+static int samsung_oneplus_dsc_bl_update_status(struct backlight_device *bl)
+{
+	struct mipi_dsi_device *dsi = bl_get_data(bl);
+	u16 brightness = backlight_get_brightness(bl);
+	int ret;
+	unsigned long saved_flags = dsi->mode_flags;
+
+	dsi->mode_flags &= ~MIPI_DSI_MODE_LPM;
+
+	ret = mipi_dsi_dcs_set_display_brightness_large(dsi, brightness);
+	dsi->mode_flags = saved_flags;
+
+	return ret;
+}
+
+static const struct backlight_ops samsung_oneplus_dsc_bl_ops = {
+	.update_status = samsung_oneplus_dsc_bl_update_status,
+};
+
+static struct backlight_device *
+samsung_oneplus_dsc_create_backlight(struct mipi_dsi_device *dsi)
+{
+	struct device *dev = &dsi->dev;
+	const struct backlight_properties props = {
+		.type = BACKLIGHT_RAW,
+		.brightness = 320,
+		.max_brightness = 1023,
+	};
+
+	return devm_backlight_device_register(dev, dev_name(dev), dev, dsi,
+					      &samsung_oneplus_dsc_bl_ops, &props);
+}
+
+static int samsung_oneplus_dsc_probe(struct mipi_dsi_device *dsi)
+{
+	struct device *dev = &dsi->dev;
+	struct samsung_oneplus_dsc *ctx;
+	int ret;
+
+	ctx = devm_drm_panel_alloc(dev, struct samsung_oneplus_dsc, panel,
+				   &samsung_oneplus_dsc_panel_funcs,
+				   DRM_MODE_CONNECTOR_DSI);
+	if (IS_ERR(ctx))
+		return PTR_ERR(ctx);
+
+	ret = devm_regulator_bulk_get_const(dev,
+					    ARRAY_SIZE(samsung_oneplus_dsc_supplies),
+					    samsung_oneplus_dsc_supplies,
+					    &ctx->supplies);
+	if (ret < 0)
+		return dev_err_probe(dev, ret, "Failed to get regulators\n");
+
+	ctx->reset_gpio = devm_gpiod_get(dev, "reset", GPIOD_OUT_HIGH);
+	if (IS_ERR(ctx->reset_gpio))
+		return dev_err_probe(dev, PTR_ERR(ctx->reset_gpio),
+				     "Failed to get reset-gpios\n");
+
+	ctx->dsi = dsi;
+	mipi_dsi_set_drvdata(dsi, ctx);
+
+	dsi->lanes = 4;
+	dsi->format = MIPI_DSI_FMT_RGB888;
+	dsi->mode_flags = MIPI_DSI_MODE_NO_EOT_PACKET |
+			  MIPI_DSI_CLOCK_NON_CONTINUOUS |
+			  MIPI_DSI_MODE_LPM;
+
+	ctx->panel.prepare_prev_first = true;
+
+	ctx->panel.backlight = samsung_oneplus_dsc_create_backlight(dsi);
+	if (IS_ERR(ctx->panel.backlight))
+		return dev_err_probe(dev, PTR_ERR(ctx->panel.backlight),
+				     "Failed to create backlight\n");
+
+	drm_panel_add(&ctx->panel);
+
+	/* This panel only supports DSC; unconditionally enable it */
+	dsi->dsc = &ctx->dsc;
+
+	ctx->dsc.dsc_version_major = 1;
+	ctx->dsc.dsc_version_minor = 1;
+
+	ctx->dsc.slice_height = 65;
+	ctx->dsc.slice_width = 720;
+	/*
+	 * TODO: hdisplay should be read from the selected mode once
+	 * it is passed back to drm_panel (in prepare?)
+	 */
+	WARN_ON(1440 % ctx->dsc.slice_width);
+	ctx->dsc.slice_count = 1440 / ctx->dsc.slice_width;
+	ctx->dsc.dsc_slice_per_pkt = 2;
+	ctx->dsc.bits_per_component = 8;
+	ctx->dsc.bits_per_pixel = 8 << 4; /* 4 fractional bits */
+	ctx->dsc.block_pred_enable = true;
+
+	ret = mipi_dsi_attach(dsi);
+	if (ret < 0) {
+		drm_panel_remove(&ctx->panel);
+		return dev_err_probe(dev, ret, "Failed to attach to DSI host\n");
+	}
+
+	return 0;
+}
+
+static void samsung_oneplus_dsc_remove(struct mipi_dsi_device *dsi)
+{
+	struct samsung_oneplus_dsc *ctx = mipi_dsi_get_drvdata(dsi);
+	int ret;
+
+	ret = mipi_dsi_detach(dsi);
+	if (ret < 0)
+		dev_err(&dsi->dev, "Failed to detach from DSI host: %d\n", ret);
+
+	drm_panel_remove(&ctx->panel);
+}
+
+static const struct of_device_id samsung_oneplus_dsc_of_match[] = {
+	{ .compatible = "samsung,oneplus-dsc" },
+	{ /* sentinel */ }
+};
+MODULE_DEVICE_TABLE(of, samsung_oneplus_dsc_of_match);
+
+static struct mipi_dsi_driver samsung_oneplus_dsc_driver = {
+	.probe = samsung_oneplus_dsc_probe,
+	.remove = samsung_oneplus_dsc_remove,
+	.driver = {
+		.name = "panel-samsung-oneplus-dsc",
+		.of_match_table = samsung_oneplus_dsc_of_match,
+	},
+};
+module_mipi_dsi_driver(samsung_oneplus_dsc_driver);
+
+MODULE_AUTHOR("hotdog-linux-bringup contributors");
+MODULE_DESCRIPTION("DRM driver for samsung dsc cmd mode oneplus dsi panel");
+MODULE_LICENSE("GPL");
--- a/include/drm/display/drm_dsc.h
+++ b/include/drm/display/drm_dsc.h
@@ -267,6 +267,12 @@
 	 * Offset adjustment for second line in Native 4:2:0 mode
 	 */
 	u16 second_line_offset_adj;
+	/**
+	 * @dsc_slice_per_pkt:
+	 * Number of DSC slices to be sent in a single packet. This is not
+	 * part of the DSC standard and is currently used by DSI panels.
+	 */
+	unsigned int dsc_slice_per_pkt;
 };
 
 /**
