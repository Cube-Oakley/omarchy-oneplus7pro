# The manually described, attenuated sink is the only validated output.
# Avoid auto-created ALSA profiles that bypass it or probe unfinished capture.
wireplumber.profiles = {
    main = {
        monitor.alsa = disabled
        monitor.bluez = disabled
    }
}
